{
    "$schema": "https://getcomposer.org/schema.json",

    "name": "diamondstrider1/pmmp-quick-friends",
    "description": "Fast friending system for PocketMine server owners! ",
    "type": "project",
    "license": "MIT",

    "require": {
        "php": "^8.0.0",
		"pocketmine/pocketmine-mp": "4.0.0"
    },

    "scripts": {
        "setup": [
            "echo includes:> phpstan.neon",
            "echo     - phpstan.neon.dist>> phpstan.neon",
            "composer install"
        ],
        "fmt": "tools/php-cs-fixer/vendor/bin/php-cs-fixer fix src",
        "phpstan": "tools/phpstan/vendor/bin/phpstan analyze -c phpstan.neon"
    },

    "config": {
        "allow-plugins": {
            "phpstan/extension-installer": true
        }
    }
}
